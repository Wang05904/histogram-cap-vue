import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {

    appId: 'com.histogram.app',

    appName: 'Histogram App',

    webDir: 'dist',

    bundledWebRuntime: false

}

export default config